# 任务执行简报

- 任务名称：Notification MCP / remove
- 任务类型：ToDo
- 当前状态：Active
- 负责人：llm
- 优先级：Low
- 起止时间：2026-3-19 至 未设置
- 关联架构对象名称与 ID：Notification MCP（1227）；ai4sec_agent（1214）；ApplicationLayer（1209）；TechnologyLayer（1210）；通知预警标准化通道（1230）；业务流程中所有数据都必须是OPENCTI平台中的STIX2.1标准数据（1226）

## 1. LLM执行摘要

- 当前任务要完成的是移除 `Notification MCP` 的运行时服务与调用链，模板资产单独评估，不在本轮一并清除。
- 首要修改对象是 `mcp/notification_mcp/app/` 下的服务实现，以及 `ai4sec_agent` 对 `NOTIFICATION_MCP_URL` 的运行时调用面。
- 不允许越界到 SMTP 模板重设计、组织架构同步或新通知平台选型；已知前提是高风险/中断级通知已有替代方案。
- 最关键验收条件是仓库运行时不再依赖 Notification MCP API，且外部系统不存在仍需调用其 HTTP 接口的前提。
- 由于原则 1230 把 Notification MCP 定义为标准化通知通道，移除后必须显式暴露原则与实现之间的暂时缺口或替代事实。
- `.env` 与 `.env.example` 当前仍保留整套 `NOTIFICATION_MCP_*` 配置，属于优先清理的运行时残留。
- `README.md`、`pyproject.toml`、技术层文档仍把该服务当作正式能力，这些内容可能滞后，但按当前任务边界优先级低于运行时清理。
- 模板资产和外部说明文件可能继续保留，例如 `external_opencti/opencti_webhook_signal_template.ejs`；本任务需避免把模板评估与服务下线混为一项。
- 当前主要风险是统一工作流若仍调用该服务，会导致运行期断链；因此需要先确认调用者，再切服务本体。

## 2. 已确认事实

- 架构元素 `Notification MCP`（1227）当前对应代码路径 `mcp/notification_mcp/app/main.py`、`service.py`、`settings.py`、`models.py`。
- `ApplicationLayer`（1209）通过关系 1089 定义 `ai4sec_agent`（1214）向 `Notification MCP`（1227）发送预警通知。
- `TechnologyLayer`（1210）明确该能力作为 MCP 节点部署，由 Dify Agent 通过 HTTP/SSE 集成，并依赖 SMTP 配置执行标准化通信投递。
- 原则 `通知预警标准化通道`（1230）规定高级别结果统一交由 Notification MCP 组件承担。
- `.env` 与 `.env.example` 中存在 `NOTIFICATION_MCP_DEFAULT_FORMAL_RECIPIENT`、`NOTIFICATION_MCP_PREVIEW_MODE`、`NOTIFICATION_MCP_SMTP_*` 等运行时配置项。
- 当前任务说明明确“先仅移除运行时服务与调用；模板资产单独评估”，且“外部系统不仍依赖 Notification MCP API”被视为任务前提。

## 3. 需人工确认 / 未知项

- 未知“高风险/中断级通知已替代”具体落在哪个运行时组件。建议把当前任务按“去除旧服务依赖、显式暴露替代未入仓或未建模事实”执行，不在本任务内补建新通知实现。
- 未知是否有仓库外系统直接调用 `mcp/notification_mcp` HTTP API。合理假设是没有，但需人工确认部署侧、联调脚本或外部 webhook 配置。
- 未知模板资产是否仍被其他链路复用。建议当前只保留模板文件并在简报中标记“待单独评估”，避免误删非服务资产。
- 未知测试目录中哪些用例仍将 Notification MCP 视为正式合同。建议先核对契约测试是否必须调整，若超出运行时清理范围则登记为后续项。

## 4. 约束与边界

- 必须遵守 `Progressive Disclosure`：先下线服务和调用，再决定模板、README、架构描述和测试是否进入后续批次。
- 必须遵守 `Separation of Concerns`：只处理 Notification MCP 运行时服务与调用关系，不扩展到 OpenCTI、MCP 查询、业务层通知策略改造。
- 必须遵守原则 1226：任何保留的业务输出仍应保持 STIX 相关对象引用语义，不应因删除通知服务而修改核心业务数据表达。
- 必须保持不变的模块边界：`mcp/opencti_mcp/app/*`、`DifyAgentWorkflow/ai4sec_opencti_webhook_workflow.yaml`、`DifyAgentWorkflow/ai4sec_threat_modeling_workflow.yaml` 不应被顺带改造。
- 明确禁止的实现方式：禁止在本任务中私自引入新的通知服务；禁止把模板资产删除当作已完成运行时下线；禁止未确认外部依赖就强行清空所有通知相关文档。
- 若无法确认替代通知出口，应明确记录为缺口或待人工确认，不得伪造“已切换完成”的结论。

## 5. 架构元素级任务拆解

| 子任务名称 | 对应架构元素 | 技术目的 | 与其他子任务的依赖关系 |
| --- | --- | --- | --- |
| 清点通知服务触点 | Notification MCP（1227）；ai4sec_agent（1214） | 找出服务实现、环境变量、调用入口与文档中的运行时暴露面 | 起始子任务 |
| 切断调用方依赖 | ai4sec_agent（1214）；Notification MCP（1227） | 先去除统一工作流对通知 API 的调用，避免服务删除后产生断链 | 依赖“清点通知服务触点” |
| 移除通知服务实现 | Notification MCP（1227）；TechnologyLayer（1210） | 删除或停用 `mcp/notification_mcp/app/*` 作为可运行服务的实现 | 依赖“切断调用方依赖” |
| 保留并标记模板资产待评估 | Notification MCP（1227）；通知预警标准化通道（1230） | 明确模板资产不在本轮清理，避免误判范围 | 可与后两项并行，但需在收尾时统一说明 |

## 6. 推荐实施顺序

1. 动作说明：检索 Notification MCP 的所有运行时调用、环境配置与暴露说明，区分服务代码、模板资产和文档描述。
   目标文件 / 模块 / 目录：`mcp/notification_mcp/app/`、`DifyAgentWorkflow/`、`.env`、`.env.example`、`README.md`
   对应架构元素 ID：1227、1214、1230
   完成判定标准：形成“服务实现 / 调用点 / 模板资产 / 文档”四类清单。
2. 动作说明：先清理调用方中的 `NOTIFICATION_MCP_URL` 或等价通知 API 依赖，避免残留运行时断链。
   目标文件 / 模块 / 目录：`DifyAgentWorkflow/ai4sec_unified_workflow.yaml`、需结合代码仓进一步定位
   对应架构元素 ID：1214、1227
   完成判定标准：仓库中不再存在面向 Notification MCP API 的活跃调用路径。
3. 动作说明：移除或停用 Notification MCP FastAPI 服务实现与其启动说明中的运行时依赖。
   目标文件 / 模块 / 目录：`mcp/notification_mcp/app/main.py`、`mcp/notification_mcp/app/service.py`、`mcp/notification_mcp/app/settings.py`、`mcp/notification_mcp/app/models.py`
   对应架构元素 ID：1227、1210
   完成判定标准：服务不再作为仓库可运行组件存在。
4. 动作说明：清理 `NOTIFICATION_MCP_*` 环境配置残留，并注明模板资产待独立评估。
   目标文件 / 模块 / 目录：`.env`、`.env.example`、需结合代码仓进一步定位
   对应架构元素 ID：1227、1230
   完成判定标准：仅保留仍被其他组件需要的配置；模板资产去留被单独标注。

## 7. 建议修改目标

- 优先检查的文件：`mcp/notification_mcp/app/main.py`；`mcp/notification_mcp/app/service.py`；`mcp/notification_mcp/app/settings.py`；`mcp/notification_mcp/app/models.py`；`.env`；`.env.example`
- 可能需要新增的文件：未知；如需记录替代通知出口缺口，可新增一次性迁移说明，但 KG 未强制。
- 可能需要避免修改的文件：`external_opencti/opencti_webhook_signal_template.ejs`；`mcp/opencti_mcp/app/*`；`DifyAgentWorkflow/ai4sec_opencti_webhook_workflow.yaml`；`DifyAgentWorkflow/ai4sec_threat_modeling_workflow.yaml`

## 8. 交付物与验收标准

- [ ] Notification MCP 服务实现已从仓库运行时路径移除或停用。

***人工验收测试步骤 (Generated by Copilot):***
1. 在仓库根目录确认 `mcp/notification_mcp/__init__.py` 与 `mcp/notification_mcp/app/` 下服务实现文件均已删除。
2. 搜索 `mcp.notification_mcp`，确认仅剩文档或 Git 变更记录，不再有可执行导入链。
- [ ] 仓库中不再保留依赖 Notification MCP API 的活跃调用链。

***人工验收测试步骤 (Generated by Copilot):***
1. 在仓库内搜索 `NOTIFICATION_MCP_URL` 与 `/dispatch`，确认保留工作流和 Python 代码中无活跃通知 MCP API 调用。
2. 确认 `tests/unit/test_runtime_tools_closed_loop.py`、`tests/integration/test_webhook_to_notification_flow.py` 与 `tests/contract/test_notification_api_contract.py` 已删除。
- [ ] `NOTIFICATION_MCP_*` 环境变量残留已清理或有明确保留理由。

***人工验收测试步骤 (Generated by Copilot):***
1. 打开 `.env` 与 `.env.example`，确认 `NOTIFICATION_MCP_*` 配置已移除。
2. 确认两个文件只保留“统一 agent 与 MCP 运行时资产已移除”的说明注释。
- [ ] 模板资产未被误删，并被明确标注为后续单独评估范围。

***人工验收测试步骤 (Generated by Copilot):***
1. 确认 `external_opencti/opencti_webhook_signal_template.ejs` 仍存在。
2. 阅读本文件第 1、3、4、9 节，确认模板资产被明确标记为“单独评估范围”，未被并入本轮运行时删除范围。
- [ ] 原则缺口、替代出口未知项和外部依赖确认项被显式记录。

***人工验收测试步骤 (Generated by Copilot):***
1. 阅读本文件第 3 节与第 9 节，确认已记录“高风险/中断级通知替代出口未知”和“仓库外系统依赖需人工确认”。
2. 阅读 `design/tasks/taskandissues_for_LLM.md` 中 1227 对应 ResolverNotes，确认缺口、验证和待确认事项已写回任务索引。

## 9. 风险、阻塞与缓解措施

- 风险：架构仍宣称 Notification MCP 是标准出口，但实现被删除后会产生原则缺口。缓解：在后续架构同步任务中补充例外说明或替代元素。
- 风险：统一工作流仍引用通知接口会导致运行时失败。缓解：按“先切调用、后删服务”的顺序执行。
- 风险：模板资产可能被错误理解为运行时服务的一部分。缓解：在任务说明与收尾中明确模板资产暂不处理。
- 阻塞：仓库无法确认外部系统是否直接调用 Notification MCP API。缓解：标记为 `需人工确认`，不把无法验证的外部依赖当作已完成事实。