# 任务执行简报

- 任务名称：全局情报底座：OPENCTI 平台
- 任务类型：ToDo
- 当前状态：Active
- 负责人：llm
- 优先级：Low
- 起止时间：2026-3-16 ~ 未知（KG 原值为 1899-12-30，按无效截止日处理）
- 关联架构对象名称与 ID：
  - 全局情报底座：OPENCTI 平台（1228）
  - ai4sec_agent（1214）
  - ai4sec_opencti_mcp（1215）
  - OpenCTI Runtime Stack（1276）
  - Dify Runtime Stack（1275）

## 1. LLM执行摘要

- 本任务目标是将 OpenCTI webhook ingress 回归到 ai4sec_agent 边界，移除 ai4sec_opencti_mcp 的旧入口路径。
- 首要修改对象是 webhook 接入链路与入参适配层，保证 STIX Bundle 能进入工作流节点。
- 范围边界是不做灰度双写、不保留 MCP 旧入口、不扩展到其他 value stream 分支复制。
- 关键验收条件是 OpenCTI 能直接推送到 Agent 新入口，且下游工作流入参转换可用。
- 需落地“Agent 中转 webhook + 工作流调用”的最终部署形态。
- 需提供 OpenCTI webhook 模板切换到 Agent 地址的可执行配置指引。
- 签名与重放防护当前无组织级统一策略，属于待确认治理项，需先显式标记风险。
- OpenCTI 推送到 Agent 不需要鉴权，但需要明确定义最小可接受请求结构和错误处理。

## 2. 已确认事实

- KG 确认存在原则元素：全局情报底座：OPENCTI 平台（1228），其描述明确内部 OpenCTI 作为情报底座。
- KG 关系 1086：OpenCTI Runtime Stack（1276）主动推送 STIX BUNDLE 到 ai4sec_agent（1214）。
- KG 关系 1082：ai4sec_agent（1214）访问 ai4sec_opencti_mcp（1215）。
- KG 关系 1083：ai4sec_opencti_mcp（1215）访问 OpenCTI Runtime Stack（1276）。
- KG 关系 1079：ai4sec_agent（1214）聚合 Dify Runtime Stack（1275）。
- 任务行状态为 Active，任务类型为 ToDo，负责人为 llm（来自元素 1228 的 project_info.tasks）。

## 3. 需人工确认 / 未知项

- Agent webhook 入口的最终 URL 路径、方法限制、请求大小上限与超时策略：未知。
- 组织级签名校验与重放防护策略（算法、时钟偏移、nonce 存储）：需人工确认。
- OpenCTI webhook 模板的现网发布清单与切换窗口：需人工确认。
- 回滚方案与发布失败时的应急流程：未知。
- 入口不鉴权前提下的网络边界与来源控制策略：需人工确认。

## 4. 约束与边界

- 强制原则：Progressive Disclosure。先完成最小可用 webhook 中转链路，再逐步补充治理能力，不得一次性并发引入多套新机制。
- 强制原则：Separation of Concerns。webhook 接入、参数适配、工作流编排、通知下发必须分层，禁止把路由与业务研判耦合到同一模块。
- 显式原则：全局情报底座：OPENCTI 平台（1228）必须保持为唯一情报底座入口语义。
- 显式原则：业务流程中所有数据都必须是OPENCTI平台中的STIX2.1标准数据（1226）对接入数据格式持续生效。
- 必须保持不变：OpenCTI Runtime Stack（1276）与 Dify Runtime Stack（1275）的职责边界。
- 明确禁止：继续保留 ai4sec_opencti_mcp 旧 webhook 外部入口并对外宣称可用。
- 明确禁止：在未确认组织级策略前假定签名/重放机制细节并写死为最终标准。

## 5. 架构元素级任务拆解

| 子任务名称 | 对应架构元素 | 技术目的 | 依赖关系 |
| --- | --- | --- | --- |
| 入口边界迁移 | ai4sec_agent（1214）, 全局情报底座：OPENCTI 平台（1228） | 将 webhook ingress 落到 Agent 边界，符合目标架构 | 先于其余子任务 |
| 旧入口下线 | ai4sec_opencti_mcp（1215） | 关闭 MCP 旧入口，避免双入口漂移 | 依赖入口边界迁移 |
| STIX 入参适配 | ai4sec_agent（1214）, Dify Runtime Stack（1275） | 将 STIX Bundle 映射为工作流节点入参 | 依赖入口边界迁移 |
| 模板切换指引 | OpenCTI Runtime Stack（1276）, 全局情报底座原则（1228） | 指导现网模板切换至 Agent 地址 | 依赖 STIX 入参适配 |
| 风险治理占位 | 全局情报底座原则（1228） | 明确签名与重放防护待确认点并设置后续动作 | 并行，可在上线前收敛 |

## 6. 推荐实施顺序

1. 动作说明：梳理并冻结“唯一 webhook 入口在 Agent”技术方案。目标文件 / 模块 / 目录：需结合代码仓进一步定位。对应架构元素 ID：1228, 1214。完成判定标准：方案文档明确入口职责和旧入口下线路径。
2. 动作说明：实现 Agent webhook 接口并完成 STIX Bundle 基础校验。目标文件 / 模块 / 目录：需结合代码仓进一步定位。对应架构元素 ID：1214, 1276。完成判定标准：OpenCTI 推送样例可被 Agent 接收并返回预期状态码。
3. 动作说明：实现 Agent 到 Dify 工作流入参适配层。目标文件 / 模块 / 目录：需结合代码仓进一步定位。对应架构元素 ID：1214, 1275。完成判定标准：工作流节点可读取到标准化入参。
4. 动作说明：下线 MCP 旧 webhook 入口并删除外部可达路由。目标文件 / 模块 / 目录：需结合代码仓进一步定位。对应架构元素 ID：1215。完成判定标准：旧入口访问失败且不再被任何上游调用。
5. 动作说明：发布 OpenCTI webhook 模板切换指引并完成一次演练。目标文件 / 模块 / 目录：需结合代码仓进一步定位。对应架构元素 ID：1276, 1228。完成判定标准：模板配置可复现、演练记录完整。

## 7. 建议修改目标

- 优先检查的文件：
  - DifyAgentWorkflow/ai4sec_unified_workflow.yaml
  - config/opencti_webhook_signal_template.ejs
  - mcp/opencti_mcp/app/main.py
- 可能需要新增的文件：
  - 需结合代码仓进一步定位（例如 Agent webhook 适配层模块）。
- 可能需要避免修改的文件：
  - tests/validation/ 下已完成的业务验收记录（非本任务目标）。

## 8. 交付物与验收标准

- [ ] Agent webhook 入口可接收 OpenCTI 推送 STIX Bundle。
  手工测试步骤：1) 在 Dify 中导入 `DifyAgentWorkflow/ai4sec_opencti_webhook_workflow.yaml`，如当前环境只能测试未发布，则先使用 debug webhook URL（示例：`http://docker-nginx-1/triggers/webhook-debug/<hook_id>`）；2) 将 OpenCTI 容器接入 Dify 网络：`docker network connect docker_default xtm-opencti-1`；（说明：该命令的作用是将名为 xtm-opencti-1 的 OpenCTI 容器加入到名为 docker_default 的 Docker 网络中。这样 OpenCTI 容器就能通过该网络与 Dify 相关容器（如 docker-nginx-1）直接通信，实现容器间服务互通。docker network connect：Docker 的命令，用于把一个已存在的容器连接到一个已存在的网络。docker_default：这是 Dify 相关服务所在的 Docker 网络（通常由 docker-compose 自动创建，名称格式为“项目名_default”）。xtm-opencti-1：这是 OpenCTI 服务对应的容器名称。）3) 在 OpenCTI 容器内执行连通性探测：`docker exec xtm-opencti-1 sh -lc "wget -T 8 -S -O- --header='Content-Type: application/json' --post-data='{}' 'http://docker-nginx-1/triggers/webhook-debug/<hook_id>'"`，预期 HTTP 200 且返回 `Webhook processed successfully`；4) 在 OpenCTI 管理台将 Notifier URL 指向同一 debug webhook 地址并触发一次包含至少 1 个 STIX object 的事件推送；5) 在 Dify 执行历史中确认该专用 webhook 工作流被触发。若后续可发布，再切换到正式地址 `/triggers/webhook/<hook_id>` 并复测。
- [ ] ai4sec_opencti_mcp 旧 webhook 入口已下线且无外部依赖。
  手工测试步骤：1) 使用 curl 或 Postman 调用旧入口 `/webhooks/opencti/threat-intelligence`（opencti_mcp）；2) 预期返回 404 或路由不存在；3) 在 OpenCTI webhook 配置中确认无旧地址残留；4) 执行一次真实推送确认业务链路不再依赖旧入口。
- [ ] STIX Bundle 到 Dify 工作流入参映射有可复用适配层。
  手工测试步骤：1) 发送一个包含 `type/id/name/x_opencti_id/x_opencti_lookup_required` 的 STIX Bundle 到该专用 Dify webhook；2) 在 Dify 调试/日志面板检查 `webhook_bundle` 输入已进入工作流；3) 确认 `Normalize Webhook Bundle` 节点成功提取 `lookup_id/object_type/selected_flow` 并传给后续节点。
- [ ] OpenCTI webhook 模板切换指引已提供并可执行。
  手工测试步骤：1) 按 `external_opencti/opencti_webhook_agent_switch_guide.md` 逐步执行切换；2) 完成后进行一次联调推送；3) 按文档校验项逐条勾选并记录结果。
- [ ] 发布文档显式记录签名/重放策略为“需人工确认”，未做虚假闭环声明。
  手工测试步骤：1) 打开切换指引与任务文档；2) 检查是否明确写出签名与重放策略待组织确认；3) 确认无“已完成签名防护”类表述；4) 在发布记录中补充该风险项。

## 9. 风险、阻塞与缓解措施

- 技术风险：入口迁移导致短时丢包。缓解：在切换前后保留可观测日志与重试策略。
- 依赖风险：现网模板未统一切换。缓解：提供批量核查清单并进行演练。
- 信息缺口：签名/重放组织级策略缺失。缓解：上线前明确最低防护要求，后续治理项纳入计划。
- 范围风险：任务被扩展到多分支复制。缓解：严格限定本次只打通主场景路径。