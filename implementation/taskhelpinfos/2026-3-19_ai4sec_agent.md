# 任务执行简报

- 任务名称：ai4sec_agent / remove
- 任务类型：ToDo
- 当前状态：Active
- 负责人：llm
- 优先级：Low
- 起止时间：2026-3-19 至 未设置
- 关联架构对象名称与 ID：ai4sec_agent（1214）；ai4sec_opencti_mcp（1215）；Notification MCP（1227）；ApplicationLayer（1209）；业务流程中所有数据都必须是OPENCTI平台中的STIX2.1标准数据（1226）；统一智能入口：DIFY Agent（1229）；通知预警标准化通道（1230）

## 1. LLM执行摘要

- 当前任务要完成的是移除统一 Dify 工作流资产 `ai4sec_agent` 及其运行时可执行路径，优先处理真实运行入口，不先展开文档与测试的收尾批次。
- 首要修改对象是 `DifyAgentWorkflow/ai4sec_unified_workflow.yaml` 与其直接依赖的 `DifyAgentWorkflow/tools/ai4sec_runtime_tools.py`。
- 任务边界是移除统一入口自身及其直连运行时依赖，不在本任务内自动补建替代入口，也不补做历史重定向。
- 必须显式暴露统一入口被删除后的能力缺口，尤其是 VS1-VS4 统一路由与统一通知出口的缺口。
- 已确认该元素在架构上承担“统一智能入口”角色，但仓库中已有独立 `ai4sec_opencti_webhook_workflow.yaml` 与 `ai4sec_threat_modeling_workflow.yaml`，因此删除不能误伤这两条独立链路。
- 若统一工作流仍调用 `OPENCTI_MCP_URL` 或 `NOTIFICATION_MCP_URL`，需一并清理其环境引用和运行时调用。
- 最关键验收条件是仓库内不再保留 `ai4sec_unified_workflow.yaml` 的运行时使用路径，且不会影响现有 webhook 告警通知路径这一已知约束。
- 当前主要风险是 `README.md`、`pyproject.toml`、架构文档仍把 `ai4sec_agent` 作为正式能力描述；这些收尾可记录为后续清理，但不能与运行时删除混淆。
- 另一个主要风险是测试或容器配置若仍假设统一工作流存在，会造成非运行时回归，需要在修改后做最小范围核对。

## 2. 已确认事实

- 架构元素 `ai4sec_agent`（1214）当前被定义为统一 Dify advanced-chat workflow 资产，代码路径明确指向 `DifyAgentWorkflow/ai4sec_unified_workflow.yaml` 与 `DifyAgentWorkflow/tools/ai4sec_runtime_tools.py`。
- `ApplicationLayer`（1209）将 `ai4sec_agent` 规定为统一智能入口，并通过关系 1082 访问 `ai4sec_opencti_mcp`（1215），通过关系 1089 访问 `Notification MCP`（1227）。
- 原则 `业务流程中所有数据都必须是OPENCTI平台中的STIX2.1标准数据`（1226）要求所有业务流转数据保持 STIX 2.1 语义，删除统一入口时不能引入绕开 OpenCTI 语义的新旁路。
- 原则 `统一智能入口：DIFY Agent`（1229）说明主动交互与被动 webhook 调度都曾统一收敛到 `ai4sec_agent`；删除该元素会直接改变当前应用层入口结构。
- 原则 `通知预警标准化通道`（1230）说明高级别结果统一通过 `Notification MCP` 承担；`ai4sec_agent` 是当前架构中触发该出口的统一入口之一。
- 仓库中仍存在独立工作流 `DifyAgentWorkflow/ai4sec_opencti_webhook_workflow.yaml` 与 `DifyAgentWorkflow/ai4sec_threat_modeling_workflow.yaml`，它们不是 `ai4sec_agent` 的同名文件。

## 3. 需人工确认 / 未知项

- 未知是否仍有外部 Dify 应用配置、导入脚本或平台侧工作流 ID 持续引用 `ai4sec_unified_workflow.yaml`。建议先在仓库与部署说明中做静态检索；若仓库外仍存在线上导入记录，按“删除文件但同步登记外部残留依赖”处理。
- 未知 `DifyAgentWorkflow/tools/ai4sec_runtime_tools.py` 是否仅被统一工作流使用。建议在仓库中确认其被调用范围；若被其他工作流复用，则只删除统一工作流专属入口，不直接删工具文件。
- 未知 `.env`、容器启动脚本或外部编排是否把统一工作流当作默认入口。合理假设是仓库内没有独立启动脚本绑定该 YAML，但需人工确认平台侧导入配置。
- 未知现有测试是否仍把统一工作流视为正式资产。建议先最小化保留测试缺口说明，不在本任务内扩展重写无关测试。

## 4. 约束与边界

- 必须遵守 `Progressive Disclosure`：先清理运行时与可执行路径，再把文档、测试、架构描述拆到后续批次，不要一次性混做所有收尾。
- 必须遵守 `Separation of Concerns`：只处理统一工作流资产及其直接运行时依赖，不把 OpenCTI 访问链路、Notification 服务实现、独立 webhook 工作流改造成新的统一入口。
- 必须遵守原则 1226：删除后保留的链路仍应围绕 OpenCTI/STIX 2.1 数据语义工作，不得引入临时非 STIX 中间格式作为持久业务接口。
- 必须保持不变的模块边界：`DifyAgentWorkflow/ai4sec_opencti_webhook_workflow.yaml`、`DifyAgentWorkflow/ai4sec_threat_modeling_workflow.yaml`、`mcp/opencti_mcp/app/*`、`mcp/notification_mcp/app/*` 不应因本任务被顺带重构。
- 明确禁止的实现方式：禁止为了兼容历史入口而新增重定向壳层；禁止在同一任务里顺手改造独立工作流以填补统一入口缺口；禁止无证据删除被其他流程复用的工具模块。
- 若无法从 KG 确认新的统一入口替代方案，应直接记录缺口，不得虚构新的架构元素或运行时路径。

## 5. 架构元素级任务拆解

| 子任务名称 | 对应架构元素 | 技术目的 | 与其他子任务的依赖关系 |
| --- | --- | --- | --- |
| 清点统一入口运行时触点 | ai4sec_agent（1214） | 确认统一工作流 YAML、工具调用点、环境变量引用与说明文档中的运行时入口 | 起始子任务 |
| 移除统一工作流资产 | ai4sec_agent（1214） | 删除 `ai4sec_unified_workflow.yaml` 或使其不再作为可执行入口存在 | 依赖“清点统一入口运行时触点” |
| 切断统一入口对 MCP/通知的调用面 | ai4sec_agent（1214）；ai4sec_opencti_mcp（1215）；Notification MCP（1227） | 清理统一入口专属的 `OPENCTI_MCP_URL` / `NOTIFICATION_MCP_URL` 使用点与导入依赖 | 依赖“移除统一工作流资产” |
| 记录删除后的架构缺口 | ApplicationLayer（1209）；统一智能入口：DIFY Agent（1229） | 明确统一入口缺失、不做重定向、后续文档与测试待清理的边界 | 依赖前两项结果 |

## 6. 推荐实施顺序

1. 动作说明：检索 `ai4sec_agent` 的真实运行时文件、环境变量和文档提及点，识别必须先处理的执行路径。
   目标文件 / 模块 / 目录：`DifyAgentWorkflow/`、`README.md`、`.env`、`.env.example`
   对应架构元素 ID：1214、1229
   完成判定标准：能列出统一入口的最小运行时触点清单，并区分“必须现在处理”与“后续批次处理”。
2. 动作说明：移除或停用统一工作流 YAML，使仓库内不再保留 `ai4sec_agent` 的可执行主入口。
   目标文件 / 模块 / 目录：`DifyAgentWorkflow/ai4sec_unified_workflow.yaml`
   对应架构元素 ID：1214
   完成判定标准：运行时资产不存在或不再被当作可执行工作流使用。
3. 动作说明：清理统一工作流专属的工具引用、MCP URL、通知 URL 或其他残留入口绑定。
   目标文件 / 模块 / 目录：`DifyAgentWorkflow/tools/ai4sec_runtime_tools.py`、`.env`、`.env.example`、需结合代码仓进一步定位
   对应架构元素 ID：1214、1215、1227
   完成判定标准：不存在仅为统一工作流保留的直连引用；若有共享工具，则完成最小保留并写明原因。
4. 动作说明：补充最小缺口说明，标记统一入口删除后的后续清理项，但不扩展到文档全面重写。
   目标文件 / 模块 / 目录：`implementation/task-list.md` 或需结合代码仓进一步定位
   对应架构元素 ID：1209、1229、1230
   完成判定标准：缺口、边界与未处理项被明确记录，无隐式兼容假设。

## 7. 建议修改目标

- 优先检查的文件：`DifyAgentWorkflow/ai4sec_unified_workflow.yaml`；`DifyAgentWorkflow/tools/ai4sec_runtime_tools.py`；`.env`；`.env.example`
- 可能需要新增的文件：未知；如需保留缺口说明，可新增一次性迁移说明文件，但当前 KG 不强制。
- 可能需要避免修改的文件：`DifyAgentWorkflow/ai4sec_opencti_webhook_workflow.yaml`；`DifyAgentWorkflow/ai4sec_threat_modeling_workflow.yaml`；`mcp/opencti_mcp/app/*`；`mcp/notification_mcp/app/*`

## 8. 交付物与验收标准

- [ ] `ai4sec_agent` 对应统一工作流入口已从运行时路径移除或停用。

***人工验收测试步骤 (Generated by Copilot):***
1. 在仓库根目录确认 `DifyAgentWorkflow/ai4sec_unified_workflow.yaml` 不存在。
2. 在仓库内搜索 `ai4sec_unified_workflow`，确认只剩任务文档或 Git 变更记录，不再有可执行运行时引用。
3. 打开 `tests/e2e/test_delivery_assets.py`，确认已删除 unified workflow 资产断言。
- [ ] 与统一工作流直接绑定的 MCP/通知调用点已清理或明确保留理由。

***人工验收测试步骤 (Generated by Copilot):***
1. 在仓库内搜索 `OPENCTI_MCP_URL`、`NOTIFICATION_MCP_URL` 和 `ai4sec_runtime_tools`，确认 Python 运行时代码与保留工作流中无活跃命中。
2. 确认 `DifyAgentWorkflow/tools/ai4sec_runtime_tools.py` 已删除。
3. 检查 `.env` 与 `.env.example`，确认只保留删除说明注释，不再保留 MCP 相关仓库级变量。
- [ ] 删除操作未修改独立 webhook 与威胁建模工作流的既有链路。

***人工验收测试步骤 (Generated by Copilot):***
1. 运行 `pytest tests/e2e/test_delivery_assets.py`。
2. 确认结果为 2 个用例通过，且失败列表为空。
3. 打开 `DifyAgentWorkflow/ai4sec_opencti_webhook_workflow.yaml` 与 `DifyAgentWorkflow/ai4sec_threat_modeling_workflow.yaml`，确认文件仍存在且未被本任务替换为新入口。
- [ ] 未对历史入口做隐式重定向或临时兼容壳层。

***人工验收测试步骤 (Generated by Copilot):***
1. 搜索 `unified workflow`、`route-intent`、`redirect`、`compat` 等相关关键词，确认仓库内没有新增替代壳层文件。
2. 检查 `DifyAgentWorkflow/` 目录，仅保留独立 webhook 与威胁建模工作流，不存在新的统一入口代理资产。
- [ ] 对文档、测试、平台侧导入残留等后续清理项有显式说明。

***人工验收测试步骤 (Generated by Copilot):***
1. 阅读本文件第 1、3、9 节，确认已记录 README、架构文档、平台侧导入残留和外部 Dify 配置待人工确认项。
2. 阅读 `design/tasks/taskandissues_for_LLM.md` 中 1214 对应 ResolverNotes，确认已记录本轮变更、验证证据与剩余人工确认事项。

## 9. 风险、阻塞与缓解措施

- 风险：统一入口删除后，应用层“统一智能入口”原则描述会与代码现状失配。缓解：在后续架构/README 清理批次中同步收敛描述。
- 风险：共享工具文件被误删可能影响其他工作流。缓解：先做调用范围确认，只删除统一入口专属调用。
- 风险：平台侧可能仍保留已导入的 unified workflow。缓解：把仓库内删除与平台侧清理分开登记，避免误认为仓库删除即完成全量下线。
- 阻塞：KG 无法确认是否存在仓库外 Dify 配置。缓解：标记为 `需人工确认`，不在本任务中臆造外部状态。