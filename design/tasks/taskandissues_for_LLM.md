# Task And Issues For LLM

| Name               | Problem | ProblemNotes                                                                                                                          | ResolverNotes | ProblemType | Status | Object_ID |
| ------------------ | ------- | ------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ----------- | ------ | --------- |
| ai4sec_agent       | remove  | remove this element and related codes.<br>- 先按“运行时与可执行路径”处理，文档与测试单独列为后续清理批次。<br>- 默认不重定向历史入口，仅移除并显式暴露缺口<br>- 删除后对告警通知路径无影响，当前没有告警通知对象 | 2026-03-19: 删除 `DifyAgentWorkflow/ai4sec_unified_workflow.yaml` 与 `DifyAgentWorkflow/tools/ai4sec_runtime_tools.py`，同时裁剪 `tests/e2e/test_delivery_assets.py` 中仅针对 unified workflow 的断言，并清理 `.env`、`.env.example` 中统一入口依赖的 MCP 环境变量。<br>验证：仓库内对 `ai4sec_unified_workflow`、`ai4sec_runtime_tools`、`OPENCTI_MCP_URL`、`NOTIFICATION_MCP_URL` 的 Python/保留工作流搜索均无活跃命中；`pytest tests/e2e/test_delivery_assets.py` 通过，结果为 `2 passed in 0.20s`。<br>结果：统一入口运行时路径已下线，未新增重定向壳层。<br>剩余人工确认：平台侧是否仍保留已导入的 unified workflow，以及 README/架构文档中的统一入口描述需后续同步。 | ToDo        | Active | 1214      |
| Notification MCP   | remove  | remove this element and related codes.<br>- 先仅移除运行时服务与调用；模板资产单独评估。<br>- 高风险/中断级通知已替代<br>- 外部系统不仍依赖 Notification MCP API。              | 2026-03-19: 删除 `mcp/notification_mcp/__init__.py` 与 `mcp/notification_mcp/app/*` 服务实现，同时删除 `tests/contract/test_notification_api_contract.py`、`tests/integration/test_webhook_to_notification_flow.py`、`tests/unit/test_notification_service.py` 及统一入口专属联动测试。<br>验证：仓库内对 `mcp.notification_mcp`、`NOTIFICATION_MCP_URL` 与 `/dispatch` 的 Python/保留工作流搜索均无活跃命中；`.env` 与 `.env.example` 中 `NOTIFICATION_MCP_*` 已清空；`pytest tests/e2e/test_delivery_assets.py` 通过，结果为 `2 passed in 0.20s`。<br>结果：Notification MCP 运行时服务与仓库内调用链已移除，模板资产保留未误删。<br>剩余人工确认：高风险通知替代出口未在仓库内建模，且仓库外系统是否直接调用旧 API 仍需人工确认。 | ToDo        | Active | 1227      |
| ai4sec_opencti_mcp | remove  | remove this element and related codes.<br>- 投影能力在各AGENT流中，回写能力不再需要<br>- 历史兼容接口外部已不再依赖                                                 | 2026-03-19: 删除 `mcp/opencti_mcp/__init__.py` 与 `mcp/opencti_mcp/app/*` 兼容服务实现，同时删除 `tests/contract/test_opencti_api_contract.py`、`tests/unit/test_opencti_projection.py` 和统一入口依赖该服务的联动测试，并清理 `.env`、`.env.example` 中 `OPENCTI_MCP_*` 配置。<br>验证：仓库内对 `mcp.opencti_mcp` 与 `OPENCTI_MCP_URL` 的 Python/保留工作流搜索均无活跃命中；保留的 `DifyAgentWorkflow/ai4sec_opencti_webhook_workflow.yaml` 与 `DifyAgentWorkflow/ai4sec_threat_modeling_workflow.yaml` 继续存在；`pytest tests/e2e/test_delivery_assets.py` 通过，结果为 `2 passed in 0.20s`。<br>结果：OpenCTI MCP 兼容服务及最后一条仓库内调用链已移除。<br>剩余人工确认：README/架构文档仍描述该服务为统一访问面，且仓库外历史兼容接口调用者仍需人工确认。 | ToDo        | Active | 1215      |
