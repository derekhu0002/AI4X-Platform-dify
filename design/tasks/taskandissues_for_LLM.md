# Task And Issues For LLM

| Name | Problem | ProblemNotes | ResolverNotes | ProblemType | Status | Object_ID |
| --- | --- | --- | --- | --- | --- | --- |
| WEBHOOK_AGENT | i need that the webhook agent analyzes the infrastrctures related with the notified vulurability pushed from opencti to give out the the impact on our product, and the risk score. | - 建议输出与业务层（1208）定义的关键业务对象和 KPI 对齐的结构化摘要，至少包含：影响范围 (Affected Infrastructures/Products)、优先级建议 (Priority Recommendation) 和 决策摘要 (Decision Summary)。优先使用一跳对象本身及其名称作为产品影响候选；若无法稳定归一，输出为“需人工确认”的影响对象清单。固定使用结构化字段输出，先给出 severity_tier、risk_score、recommended_action 与 business_impact_summary 四类核心字段。<br>- 建议从“一跳关联”开始，即只查询与漏洞直接关联的 Infrastructure 和 Software 、system实体。<br>- 高风险结果不需要升级或通知协同出口<br>- 最佳实践是基于行业标准（CVSS）进行技术严重性评估，然后利用企业内部的**业务上下文（Business Context）和威胁情报（Threat Intelligence）**对其进行加权和调整，最终得出一个真正反映该漏洞对“我”企业风险的“情境化风险评分”。<br>### 1. 风险评分的量纲：建议采用 1-100 分的数值评分体系<br>**计算公式建议（示例）：**<br>`AI4SEC Risk Score = (CVSS Base Score * 10) * Asset_Criticality_Multiplier * Exposure_Multiplier * Threat_Intel_Multiplier`<br>*   **CVSS Base Score * 10**: 将 CVSS 基础分（0-10）映射到 0-100 的基准。<br>*   **Asset Criticality Multiplier (资产关键度乘数)**:<br>    *   核心业务资产（如支付网关）：**1.5**<br>    *   重要业务资产（如用户中心）：**1.2**<br>    *   一般资产：**1.0**<br>    *   非关键资产（如测试服务器）：**0.8**<br>*   **Exposure Multiplier (暴露面乘数)**:<br>    *   面向互联网直接暴露：**1.5**<br>    *   面向内网，但可从办公网段访问：**1.1**<br>    *   隔离环境/核心生产区：**0.9**<br>*   **Threat Intel Multiplier (威胁情报乘数)**:<br>    *   已确认有在野利用（Active Exploitation）：**2.0**<br>    *   已有公开PoC，但无在野利用证据：**1.5**<br>    *   无公开利用代码：**1.0**<br>缺失乘数视为 1.0，并在输出中显式标注缺失项，避免阻塞链路。先完成一跳取数与最小评分链路，再按缺口补充必要字段，而不是预先扩大全图查询范围。<br>### 2. 分档规则：基于 1-100 分的数值范围进行划分<br><br>将最终计算出的 `AI4SEC Risk Score` 映射到您系统中已有的 `severity_tier`。<br><br>\| 数值范围 \| 风险等级 (Severity Tier) \| 英文标识 \| 业务定义 \|<br>\| :--- \| :--- \| :--- \| :--- \|<br>\| **90 - 100+** \| **中断级** \| `Critical` \| 对核心业务构成直接、即时的中断威胁；或存在已被积极利用的高危漏洞。 \|<br>\| **70 - 89** \| **高** \| `High` \| 严重影响关键业务功能、可能导致数据泄露或系统权限丢失，需要紧急处理。 \|<br>\| **40 - 69** \| **中** \| `Medium` \| 可能对业务造成一定影响，或利用条件较为苛刻，应在常规补丁周期内修复。 \|<br>\| **1 - 39** \| **低/信息** \| `Low / Normal` \| 风险较低，影响有限，可机会性修复或接受风险。 \|<br>### 3. 阈值口径：将风险等级直接与处置动作和 SLA 挂钩<br><br>阈值不仅仅是一个数字，它更是行动的触发器。<br><br>\| 风险等级 \| 处置动作阈值 (Action Threshold) \| 建议的处置服务等级协议 (SLA) \|<br>\| :--- \| :--- \| :--- \|<br>\| **中断级 (Critical)** \| **立即行动**：触发自动化响应（如虚拟补丁、防火墙封禁）、创建最高优先级（P0）事件单、**立即通知**管理层（1234）和安全/业务负责人。 \| **24小时内**完成初步缓解，**7天内**完成根除。 \|<br>\| **高 (High)** \| **紧急处理**：创建高优先级（P1）事件单，由**安全运营团队（1235）**主导响应，并通知相关**业务负责人（1232）**。 \| **72小时内**完成初步缓解，**30天内**完成根除。 \|<br>\| **中 (Medium)** \| **计划内修复**：进入常规漏洞管理或补丁管理流程，无需立即告警，由**情报分析师（1231）**跟踪。 \| **90天内**完成修复。 \|<br>\| **低/信息 (Low / Normal)** \| **按需处理或接受风险**：归档记录，仅在有额外资源或作为其他修复任务的附加项时处理。 \| **180天内**或定义为可接受风险。 \|<br><br>- OpenCTI 中存在业务关键度等信息，则用其加权计算风险；如果不存在，流程应能继续，但需在输出中明确标注“业务上下文缺失，风险评估可能不完整”。<br>- 产品影响对象在 OpenCTI 中最终应优先落为 Infrastructure、Software 还是 system / Identity 投影，建议先以一跳对象名称作为候选，无法归一时输出“需人工确认”的对象清单。<br>- severity_tier 先按任务给定阈值做独立映射，再与现有输出字段名保持一致。 |  | ToDo | Active | 1277 |
| WEBHOOK_AGENT | i need webhook agent access the opencti platform with a http node in the workflow instead of the current seperate opencti mcp. | - 直连 OpenCTI 时应使用 GraphQL 接口，以 ai4sec_threat_modeling_agent（1436）现有直连查询路径作为对齐基线，若 webhook 场景确有额外字段需求，必须在实现说明中显式列出差异。<br>- 工作流内 HTTP 节点所需的认证注入方式、秘钥管理方式采用明文方式，优先复用现有 VS1 直连工作流的 OPENCTI_GRAPHQL_URL / OPENCTI_ADMIN_TOKEN 命名；如果 webhook 工作流不能完全复用，需把差异标注为实现假设。<br>- 在 ai4sec_opencti_webhook_workflow.yaml 中定位当前 MCP 工具调用节点，以此节点承接 GraphQL HTTP 请求。<br>- 迁移后需要的最小字段集合和关系深度和失败重试语义先按 1436 基线实现，仅当 webhook 下游确有缺口时再补字段，并逐项说明用途。<br>- 需要对 HTTP 查询结果做与现有兼容服务完全相同的投影合同，先以现有兼容服务返回合同为准做一一对齐，新增字段必须说明用途与下游消费者。<br>- 只移除 WEBHOOK_AGENT 的运行时依赖，不默认删除或改写 ai4sec_opencti_mcp 其他兼容路径。<br>- HTTP 节点失败重试、超时与错误码在以当前工作流实际可配置能力为上限，不额外引入仓外补偿逻辑。 |  | ToDo | Active | 1277 |
| ai4sec_threat_modeling_agent | add a new llm node responsible for summarize the current structure outpu into a graph and several tables | 1. render the visual graph representing the entities and relationships between them.<br>2. print the tables with each table representing a type of entity, such as attack pattern, course of action, indicator, attack path etc.<br>- 关系图的最终承载形式是 Mermaid，先以 STIX 对象名称和 relationship_type 生成最小关系图<br>- 表格仅在会话中展示，不需要追加到当前 JSON 下载文件中，图表结果不需要单独文件或单独变量输出，把图表作为主分析结果，当前JSON放在后面作为附加展示段落<br>- `indicator` 与 `attack path` 在当前 VS1 证据集中不会稳定存在，缺失时不显示即可<br>- 新增 LLM 节点应放在现有主分析节点之后，LLM 节点负责摘要生成，必要时用后置代码节点做轻量格式整理，但不要反转主次职责。<br>- 表格结果采用 Markdown 表格格式输出，默认使用最小公共列，例如 name、id、type、description、relationship_count；若某类型没有该列则按最小可展示列降级。 |  | ToDo | Active | 1436 |
