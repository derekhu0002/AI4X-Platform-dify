# Task And Issues For LLM

| Name | Problem | ProblemNotes | ResolverNotes | ProblemType | Status | Object_ID |
| --- | --- | --- | --- | --- | --- | --- |
| ai4sec_opencti_mcp | 将该MCP功模块替换成该工作流中的节点，所有功能通过节点来实现，不再使用该外部FASTAPI服务 | - 只需要在ai4sec_threat_modeling_agent的工作流中进行实现 | [2026-03-17] from LLM: Replaced the VS1 workflow dependency on the external FastAPI MCP with direct OpenCTI GraphQL access implemented inside `DifyAgentWorkflow/ai4sec_threat_modeling_workflow.yaml`, using workflow-level `OPENCTI_GRAPHQL_URL` and `OPENCTI_ADMIN_TOKEN` environment variables and a code node that resolves report input, fetches STIX evidence, filters the bundle, and feeds the LLM node without calling `/query/threat-model-report`.<br>Changed files: `DifyAgentWorkflow/ai4sec_threat_modeling_workflow.yaml`, `mcp/opencti_mcp/app/models.py`, `mcp/opencti_mcp/app/service.py`, `tests/unit/test_opencti_projection.py`.<br>Verification evidence: focused pytest run `d:/projects/AI4X/AI4X-Platform-dify/.venv/Scripts/python.exe -m pytest tests/unit/test_opencti_projection.py tests/contract/test_opencti_api_contract.py` passed with 21 tests; YAML parse verification confirmed `query_threat_model_report` is a `code` node and the workflow environment variables are now `OPENCTI_GRAPHQL_URL` and `OPENCTI_ADMIN_TOKEN`.<br>Result: VS1 主链路已切换为工作流内节点直连 OpenCTI，1215 不再是该工作流的运行时依赖； remaining blocker: Dify UI import/runtime confirmation still requires a human to import and run the workflow in the target Dify environment. | ToDo | Active | 1215 |
| ai4sec_opencti_mcp | 在查询和匹配OPENCTI中的report时，我希望你不要把所有report都查询回来，而是用手中的report id作为参数去查。 | - OpenCTI 中 report 精确查询使用的具体字段名为id或name，如果有ID则优先用id<br>- OpenCTI 当前运行版本对 Report 的按 name 精确查询已有稳定 GraphQL 查询入口<br>- 在 live mode 下，当输入既不是标准 report ID 也不是精确 name 时，不允许 fuzzy fallback<br>- 若 report ID 来自本地测试 bundle 而非 live standard_id，alias 映射应继续保留为兼容层<br>- 本任务所需的单对象查询限额保持和应用层合同中的OpenCTI 查询配额与分页边界相同 | [2026-03-17] from LLM: Refactored `mcp/opencti_mcp/app/service.py` from list-all-plus-fuzzy matching to exact-only report resolution with `_query_live_report_by_id` and `_query_live_report_by_exact_name`, kept alias compatibility through local bundle report-name resolution, and removed fuzzy match strategies from `mcp/opencti_mcp/app/models.py`. The VS1 workflow direct-query node in `DifyAgentWorkflow/ai4sec_threat_modeling_workflow.yaml` was aligned to the same exact-ID / exact-name / alias-only behavior so the runtime path no longer depends on list-all report scans.<br>Changed files: `mcp/opencti_mcp/app/models.py`, `mcp/opencti_mcp/app/service.py`, `DifyAgentWorkflow/ai4sec_threat_modeling_workflow.yaml`, `tests/unit/test_opencti_projection.py`.<br>Verification evidence: focused pytest run `d:/projects/AI4X/AI4X-Platform-dify/.venv/Scripts/python.exe -m pytest tests/unit/test_opencti_projection.py tests/contract/test_opencti_api_contract.py` passed with 21 tests; live GraphQL probes against `http://localhost:8080/graphql` confirmed `stixCoreObject(id: 'report--cdd45eb6-f1cd-4f17-82b3-ac4bc03fcd58')` resolves the target report and `reports(filters: { key: name, operator: eq })` returns exact-name matches without requiring a full report list query.<br>Result: report 查询已收敛到精确 ID / 精确名称 / alias 兼容路径，live mode 下部分关键词输入会直接失败而不会触发 fuzzy fallback； next action: human runtime validation inside Dify to confirm the new error/answer behavior in the UI. | ToDo | Active | 1215 |
