# Task And Issues For LLM

| Name | Problem | ProblemNotes | ResolverNotes | ProblemType | Status | Object_ID |
| --- | --- | --- | --- | --- | --- | --- |
| ApplicationLayer | 请基于现有应用层架构，结合战略动机层和业务层的设计输出应用层架构设计文档 | 战略动机层见：StrategyLayerAndMotivationAspect元素下的子图<br>业务层见：BusinessLayer元素下的子图<br>输出到design\applicationlayer下《AI4sec应用层架构设计.md》，尽量用较多的Mermaid 或表格，比例不限，目的是让人能快速理解。<br>模板不限，需要提现渐进式披露和关注点分离原则<br>先交付 Markdown 文档。<br>区分“各类情报消费者”在应用层中的细分角色映射，例如研发安全负责人、SOC 经理、管理层、业务负责人在各场景中的入口差异，需结合业务层角色与价值流故事整理<br>应用层要呈现到何种深度的接口契约、输入输出载荷或消息协议，需补充到 API 级别<br>设计各类消费者在 Dify UI、Agent 工作流<br>通知通道中权限控制暂不做<br>“各类情报消费者”在应用层中的正式角色命名要完全沿用业务层角色<br>API 级别建议统一细化到“接口目录 + 调用方/被调用方 + 认证方式 + 请求体/响应体关键字段 + 异步事件消息体 + 错误语义 + 版本策略”七段式；同步接口至少写到资源名、关键字段、必填项、幂等要求，异步消息至少写到事件名、触发条件、STIX 对象类型、重试语义。若正文篇幅受限，可只对 VS1 至 VS4 各挑 1 条主链路做完整模板，其余链路复用统一约定。<br>应用层建议显式描述 OpenCTI webhook 回查缓存、GraphQL 查询限流、字段裁剪策略，但粒度控制在“接口契约约束”而不是“实现细节”。推荐写法是：把回查缓存定义为 Agent 侧性能与幂等保护机制；把限流定义为 OpenCTI 查询配额与退避要求；把字段裁剪定义为 MCP 的响应投影能力，并在契约中只写调用方可见行为、触发条件和失败处理，不展开缓存结构、限流算法或具体代码。<br>通知通道建议在不做权限控制设计的前提下，仍补齐最小通知契约：模板粒度按“发布判定/事件响应/漏洞预警/监控上线”4 类定义，收件人分层按“主责人、审批人、抄送人”3 层定义，升级矩阵按“普通、高、中断级”3 档定义触发条件与升级路径。正文中只需说明角色映射、通知内容结构和升级规则边界，不下沉到 RBAC、组织架构同步或 SMTP 配置实现。<br>四条价值流中每条“主链路”要选VS4-E2E_环境感知监控闭环_端到端用户故事作为完整七段式接口契约样板<br>- API 目录按“共享能力 + 价值流场景”两级分组：`/api/v1/tasks`、`/api/v1/context`、`/api/v1/knowledge`、`/api/v1/notifications` 作为共享域；`/api/v1/threat-modeling` 对应 VS1、`/api/v1/incident-response` 对应 VS2、`/api/v1/vulnerability-intelligence` 对应 VS3、`/api/v1/monitoring-rules` 对应 VS4。<br>   - 异步入口与系统回调独立分组：保留 webhook 目录在 `/webhooks/opencti/*`、`/webhooks/pipeline/*`、`/webhooks/siem/*`，避免与人机交互型 API 混杂。<br>   - 版本策略建议采用“URI 主版本 + 事件名主版本”双轨制：HTTP 接口统一 `/api/v1/...`；异步事件命名采用 `ai4sec.&lt;domain&gt;.&lt;event&gt;.v1`，例如 `ai4sec.incident-response.case-created.v1`。<br>   - 小版本兼容变更不改 URI，只在文档中记录 `schema_revision` 或 `x-contract-revision`；只有字段含义破坏性变化时再升到 `v2`。<br>   - 错误码体系建议采用“域前缀 + 四位数字”统一规则：`VAL-4001` 参数校验失败、`AUTH-4010` 身份校验失败、`AUTH-4030` 权限不足、`CTI-4041` STIX 对象不存在、`MCP-4090` 字段投影冲突、`RATE-4290` 查询配额触发、`ASYNC-4990` 异步处理超时、`UPSTREAM-5020` OpenCTI / 外部上游异常、`NOTIFY-5030` 通知投递失败。<br>   - 接口路径命名规则建议统一使用小写、kebab-case、复数资源名、避免动作动词进入路径；动作语义放在 HTTP 方法或子资源中，例如 `POST /api/v1/incident-response/cases`、`POST /api/v1/monitoring-rules/rules/{rule_id}/publish`。<br>- Dify UI 中各角色入口的页面级 IA、菜单层级和交互颗粒度，建议结合业务角色采用“统一壳层 + 角色工作台 + 场景中心”结构：<br>   - 一级菜单建议固定为：`工作台`、`情报任务`、`场景中心`、`知识与证据`、`通知中心`、`治理与审计`。<br>   - `工作台` 作为角色化首页：情报分析师默认看到待研判任务、跨价值流待办和知识回灌入口；业务负责人默认看到业务影响摘要、发布判定待确认、漏洞窗口期建议；安全负责人默认看到高风险审批、升级决策、跨团队处置视图；管理层默认看到企业影响摘要、优先级决策和中断级态势卡片；安全运营团队默认看到事件分诊、规则上线、命中反馈和行动执行卡片。<br>   - `场景中心` 下建议按 VS1 至 VS4 直接分栏：`威胁建模`、`运营响应`、`漏洞影响`、`环境监控`。每个场景内部再固定为“任务列表 -&gt; 详情页 -&gt; Agent 对话侧栏 -&gt; 证据抽屉”四层，不做更深菜单树。<br>   - 页面级 IA 建议遵循“列表页看分流，详情页看证据，侧栏做交互，抽屉看 STIX 对象关系”的颗粒度，避免把长链路完全塞进聊天界面。<br>   - 角色差异建议体现在默认落点、可见卡片、审批动作和通知订阅范围，不建议拆成 5 套完全不同导航；这样更符合 Progressive Disclosure，也避免 Dify UI 维护成本失控。<br>   - 交互颗粒度建议：高频动作保持单页闭环，例如“确认影响级别”“批准升级”“发布监控规则”；复杂推理与追问放入 Agent 对话侧栏，不让业务角色直接操作底层 STIX 字段。<br><br> `ai4sec_opencti_mcp` 当前支持哪些响应字段投影配置项、失败码、分页约束，建议在应用层文档中定义如下推荐契约：<br>   - 字段投影支持两层模式：预置投影档位 + 显式字段白名单。预置档位建议至少有 `minimal`、`summary`、`analysis`、`graph`、`notification` 五档。<br>   - `minimal` 只返回 `id`、`type`、`standard_id`、`name`、`modified`；`summary` 增加 `description`、`labels`、`confidence`、关键外部引用；`analysis` 增加 `object_marking_refs`、关键关系摘要、核心扩展字段；`graph` 返回对象加一跳关系；`notification` 仅保留通知模板需要的标题、摘要、优先级、目标角色字段。<br>   - 显式投影参数建议采用：`projection_profile`、`fields[]`、`exclude_fields[]`、`include_relationships`、`relationship_depth`、`relationship_types[]`。其中 `relationship_depth` 默认 `0`，最大建议 `1`，避免应用层查询失控。<br>   - 失败码建议至少定义：`MCP-4001` 投影档位不存在、`MCP-4002` 字段名非法、`MCP-4003` 字段与对象类型不匹配、`CTI-4041` 对象不存在、`CTI-4091` 对象状态冲突、`RATE-4290` OpenCTI 查询限流、`UPSTREAM-5020` GraphQL 上游失败、`ASYNC-5040` 查询超时。<br>   - 分页约束建议统一采用 cursor 模式，而不是 offset 模式：入参 `page_size`、`cursor`；出参 `items`、`next_cursor`、`has_more`。`page_size` 默认 `20`、最大 `100`；`graph` 档位默认 `10`、最大 `30`。<br>   - 为适配业务角色，建议 UI 和 Agent 默认不直接请求 `graph` 全量明细，而先取 `summary` / `notification`，只有情报分析师在证据抽屉中再按需请求 `analysis` 或 `graph`。<br><br> Notification MCP 的模板字段、升级矩阵具体阈值、抄送策略字段名和默认值，建议在应用层文档中给出一套最小但可执行的通知合同：<br>   - 模板类型固定 4 类：`release-gate-decision`（发布判定）、`incident-response`（事件响应）、`vulnerability-alert`（漏洞预警）、`monitoring-rule-online`（监控上线）。<br>   - 通用模板字段建议统一为：`template_type`、`message_title`、`message_summary`、`business_impact`、`recommended_action`、`evidence_link`、`primary_role`、`approver_role`、`cc_roles[]`、`severity_tier`、`escalate_after_minutes`、`event_type`、`object_refs[]`、`dedup_key`、`channels[]`。<br>   - 角色映射字段名建议固定为：`primary_recipients`、`approver_recipients`、`cc_recipients`。默认值建议：`primary_recipients` 必填；`approver_recipients` 默认空数组；`cc_recipients` 默认空数组；`channels` 默认 `["email"]`。<br>   - 升级矩阵建议按 3 档定义：`普通` 适用于单系统、非生产或中风险事项，`escalate_after_minutes=240`；`高` 适用于生产关键资产、高风险结论或跨团队协同事项，`escalate_after_minutes=30`；`中断级` 适用于生产核心链路中断、重大漏洞已确认影响核心业务、或正在发生的高置信事件，`escalate_after_minutes=10`。<br>   - 升级路径建议：`普通` 仅通知主责人；`高` 通知主责人并升级到审批人，同时抄送对应业务负责人 / 安全负责人；`中断级` 直接通知主责人、审批人、管理层与安全运营团队值守角色。<br>   - 抄送策略建议默认最小化：只有满足“跨团队依赖”“需业务窗口确认”“需管理层知悉”三类条件之一时才填充 `cc_recipients`，避免所有通知泛抄送导致噪声。<br>   - 去重与幂等建议通过 `dedup_key = {template_type}:{object_id}:{severity_tier}:{business_date}` 实现合同级约束，避免同一 STIX 对象短时间重复轰炸。 | [2026-03-13] from LLM: 已完成 `design/applicationlayer/AI4sec应用层架构设计.md`。技术方案基于 ApplicationLayer 视图 164、关系 1082/1083/1086/1088/1089、业务层角色模型及 VS1-VS4 用户故事，按 Progressive Disclosure 与 Separation of Concerns 组织为角色入口与页面 IA、应用服务分层、共享 API / 场景 API / webhook 分组、`ai4sec_opencti_mcp` 投影合同、Notification MCP 最小通知合同，以及 VS4 七段式接口契约样板。变更文件：`design/applicationlayer/AI4sec应用层架构设计.md`、`design/tasks/taskandissues_for_LLM.md`。验证证据：已对新文档执行诊断检查，未发现错误；并按 `implementation/taskhelpinfos/2026-3-13_ApplicationLayer.md` 第 8 节逐项核对，已覆盖统一壳层、角色工作台、场景中心、VS1-VS4 应用层映射、共享域与场景域 API 目录、同步/异步契约样板、MCP 投影分页错误语义、通知模板与升级矩阵、以及边界约束。结果：Markdown 交付已完成。剩余阻塞：认证机制、MCP 真实实现边界、通知模板现有配置仍需后续结合代码确认；下一步可据此开展实现或评审。
[2026-03-13] from LLM: 根据用户澄清，已修正文档中的一个关键语义偏差：DIFY 被明确视为 Agent 工作流编排平台，承载 Chatflow / Workflow 与拖拽节点编排，因此不应把平台内部交互主入口表述成独立业务 REST 资源接口。已将 `design/applicationlayer/AI4sec应用层架构设计.md` 中原有 `/api/v1/threat-modeling`、`POST /api/v1/monitoring-rules/rule-drafts` 等表述，调整为“Dify Chat 页面 + Chatflow / Workflow + webhook 入口 + MCP 工具契约”的表达；保留 webhook 事件入口与 MCP 投影合同，因为这两类契约仍然需要。验证证据：文档诊断无错误，且关于 VS4 的七段式样板现已改为编排契约而非 REST 资源契约。结果：当前文档与“DIFY 是工作流编排平台”的理解已对齐。 | ToDo | Active | 1209 |
