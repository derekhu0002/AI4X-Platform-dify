# Task And Issues For LLM

| Name | Problem | ProblemNotes | ResolverNotes | ProblemType | Status | Object_ID |
| --- | --- | --- | --- | --- | --- | --- |
| ApplicationLayer | 请按架构设计实现该系统 | 基于ApplicationLayer和TechnologyLayer中的架构设计描述实现该系统，并添加完整的多层自动测试用例<br>DifyAgentWorkflow 目录中应采用的最终交付格式是 YAML（DSL文件），可用于直接导入DIFY平台创建工作流<br>mcp/opencti_mcp 与 mcp/notification_mcp 后端采用FASTAPI框架，使用PYTHON开发<br>ai4sec_agent (1214) 的运行代码是独立可导入工作流资产交付。<br>OpenCTI webhook 到 Dify 当前不需要鉴权<br>Notification MCP 的正式收件人为hdhscu@126.com，支持静态配置<br>自动测试的目标层次划分需要覆盖单元测试、契约测试、集成测试和端到端测试全部层级。<br> DifyAgentWorkflow 下具体需要使用一条 Workflow/Chatflow DSL，通过入口菜单选择不同流程，也支持通过意图识别自动选择流程<br>ai4sec_agent 独立交付的具体运行时形式纯 DIFY DSL 资产并附带 Python 工具代码<br>Notification MCP 除静态正式收件人外，需保留抄送、升级矩阵和多模板能力的运行时配置方式<br>自动测试的框架选型、CI 执行方式、契约测试基线样例和 E2E 驱动方式请帮我设计和实现 | <br>[2026-03-14] from LLM: 已按 ApplicationLayer 任务落地一条统一 Dify YAML/DSL 主工作流、两个 FastAPI MCP 服务、Webhook 解析工具代码、四层自动化测试与 CI 基线。技术方案为：在 `DifyAgentWorkflow/ai4sec_unified_workflow.yaml` 中提供菜单选择 + 意图识别双路由入口，并声明 `/webhooks/opencti/threat-intelligence` 当前无需鉴权；在 `DifyAgentWorkflow/tools/ai4sec_runtime_tools.py` 中实现 OpenCTI signal 回查、场景分流与通知载荷构造；在 `mcp/opencti_mcp/app` 中实现 STIX 投影查询、Bundle 写回、合同错误码与 webhook passthrough；在 `mcp/notification_mcp/app` 中实现正式收件人、抄送、升级矩阵、多模板与幂等去重；在 `tests/unit`、`tests/contract`、`tests/integration`、`tests/e2e` 中覆盖单元、契约、集成与端到端四层验证，并新增 `.github/workflows/ci.yml` 作为 CI 执行路径。变更文件：`pyproject.toml`、`DifyAgentWorkflow/ai4sec_unified_workflow.yaml`、`DifyAgentWorkflow/tools/ai4sec_runtime_tools.py`、`mcp/opencti_mcp/app/*.py`、`mcp/notification_mcp/app/*.py`、`tests/**/*.py`、`.github/workflows/ci.yml`。验证证据：编辑器诊断无错误；使用工作区虚拟环境执行 `pytest`，结果 `8 passed in 0.69s`。最终结果：任务验收项中的统一 YAML/DSL、FastAPI MCP、无鉴权 webhook 链路、四层测试和 CI 路径已具备最小可运行交付。剩余阻塞：当前仓内尚未接入真实 Dify 导出校验器与真实 OpenCTI GraphQL 凭据，因此运行时默认使用 mock/preview 模式；下一步应在联调环境填充 OpenCTI 与通知通道配置后做真实联调。 | ToDo | Active | 1209 |
